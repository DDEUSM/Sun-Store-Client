export type PaginationType = {
    tot_pages: number;
    actual_page: number;
    count_items: number;
}

export type WindowControlsType = {
    categorySearchWindow: boolean;
    searchBar: boolean;
    filtersWindow: boolean;
}